<form method="post" action="{{ path('app_conteneur_corde_delete', {'id': corde.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ corde.id) }}">
    <button class="btn">Delete</button>
</form>
