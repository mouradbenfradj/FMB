{% extends 'base.html.twig' %}

{% block title %}Poche index{% endblock %}

{% block body %}
    <h1>Poche index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for poche in poches %}
            <tr>
                <td>{{ poche.id }}</td>
                <td>
                    <a href="{{ path('app_conteneur_poche_show', {'id': poche.id}) }}">show</a>
                    <a href="{{ path('app_conteneur_poche_edit', {'id': poche.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_conteneur_poche_new') }}">Create new</a>
{% endblock %}
