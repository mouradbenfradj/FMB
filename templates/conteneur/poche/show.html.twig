{% extends 'base.html.twig' %}

{% block title %}Poche{% endblock %}

{% block body %}
    <h1>Poche</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poche.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_conteneur_poche_index') }}">back to list</a>

    <a href="{{ path('app_conteneur_poche_edit', {'id': poche.id}) }}">edit</a>

    {{ include('conteneur/poche/_delete_form.html.twig') }}
{% endblock %}
