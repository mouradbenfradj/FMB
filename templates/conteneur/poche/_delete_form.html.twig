<form method="post" action="{{ path('app_conteneur_poche_delete', {'id': poche.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ poche.id) }}">
    <button class="btn">Delete</button>
</form>
