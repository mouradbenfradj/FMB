{% extends 'base.html.twig' %}

{% block title %}Lanterne{% endblock %}

{% block body %}
    <h1>Lanterne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lanterne.id }}</td>
            </tr>
            <tr>
                <th>NomLanterne</th>
                <td>{{ lanterne.nomLanterne }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_conteneur_lanterne_index') }}">back to list</a>

    <a href="{{ path('app_conteneur_lanterne_edit', {'id': lanterne.id}) }}">edit</a>

    {{ include('conteneur/lanterne/_delete_form.html.twig') }}
{% endblock %}
