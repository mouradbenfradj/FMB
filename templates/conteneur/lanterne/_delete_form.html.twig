<form method="post" action="{{ path('app_conteneur_lanterne_delete', {'id': lanterne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lanterne.id) }}">
    <button class="btn">Delete</button>
</form>
